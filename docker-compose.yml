version: '3'

services:
  bot:
    build:
      context: bot
      dockerfile: bot/Dockerfile_bot
    # Добавьте любые другие настройки для вашего бота

  model:
    build:
      context: bot
      dockerfile: bot/Dockerfile_model
    # Добавьте любые другие настройки для вашей модели
